#include "src/types/binary_sensor/web_styles.h"

// The editable CSS lives in src/web/assets/admin.css and is served as one
// precompressed, cacheable firmware asset.
void append_binary_sensor_styles(String&) {}
